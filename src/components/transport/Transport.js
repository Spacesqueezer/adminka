import styled from "styled-components";

const Wrapper = styled.div``;


const TransportScreen = () => {
    return (
        <Wrapper>
            Тут будут транспорты
        </Wrapper>
    );
};

export default TransportScreen;
